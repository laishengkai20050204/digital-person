# 结构化记忆启用清单

1. 先部署包含 Flyway V6 的版本，确认三张结构化记忆表创建成功。
2. 保持 `STRUCTURED_MEMORY_ENABLED=false`，通过受保护的内部测试接口写入实体、别名和事实样本。
3. 验证错字实体解析、有效期过滤、幂等事实更新和 Mem0 降级行为。
4. 开启 `STRUCTURED_MEMORY_ENABLED=true`，观察结构化来源与 Mem0 的混合召回结果。
5. 自动对话事实提取尚未启用；在独立提取流程上线前，不应把每轮原始消息直接升级为永久结构化事实。

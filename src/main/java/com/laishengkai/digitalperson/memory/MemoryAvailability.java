package com.laishengkai.digitalperson.memory;

/** Distinguishes an empty result from a disabled or unavailable provider. */
public enum MemoryAvailability {
    AVAILABLE,
    DISABLED,
    UNAVAILABLE
}
